{
  "name": "wikidata-token",
  "version": "1.0.5",
  "description": "a promises-based lib abstracting authentification for write actions on the Wikidata API",
  "main": "build/wikidata-token.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build; ./node_modules/coffee-script/bin/coffee -o build -c src",
    "build-watch": "rm -rf build; ./node_modules/coffee-script/bin/coffee --watch -o build -c src",
    "prepublish": "npm run build"
  },
  "author": "maxlath",
  "license": "MIT",
  "dependencies": {
    "bluereq": "^2.0.1",
    "colors": "^1.1.2"
  },
  "devDependencies": {
    "coffee-script": "^1.10.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/maxlath/wikidata-token.git"
  },
  "keywords": [
    "wikidata",
    "auth",
    "token"
  ],
  "bugs": {
    "url": "https://github.com/maxlath/wikidata-token/issues"
  },
  "homepage": "https://github.com/maxlath/wikidata-token"
}
